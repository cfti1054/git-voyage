# GitHub personal access token (classic or fine-grained).
# Classic: no extra scopes needed for public contribution calendars.
# Fine-grained: grant "Profile" read access (read:user).
# Create at https://github.com/settings/tokens
# Required only when loading a real GitHub username.
GITHUB_TOKEN=
