# Server-side GitHub token for public contribution lookups.
# Use a dedicated account and do not grant private repository access.
# Classic: no scopes. Fine-grained: public repositories, read-only.
# Create at https://github.com/settings/tokens
# Local: copy this file to .env.local.
# Railway: add the same variable in the service Variables settings.
GITHUB_TOKEN=
